document.addEventListener('DOMContentLoaded',()=>{const search=document.querySelector('#news-search');const filters=[...document.querySelectorAll('.filter')];const items=[...document.querySelectorAll('.searchable')];const empty=document.querySelector('#empty-state');let active='all';const apply=()=>{const q=(search?.value||'').trim().toLowerCase();let shown=0;items.forEach(item=>{const categories=(item.dataset.category||'').split(/\s+/);const text=(item.dataset.search||item.textContent||'').toLowerCase();const okCategory=active==='all'||categories.includes(active);const okSearch=!q||text.includes(q);const visible=okCategory&&okSearch;item.hidden=!visible;if(visible)shown++;});if(empty)empty.hidden=shown!==0;};filters.forEach(btn=>btn.addEventListener('click',()=>{active=btn.dataset.filter||'all';filters.forEach(x=>x.classList.toggle('is-active',x===btn));apply();}));search?.addEventListener('input',apply);});